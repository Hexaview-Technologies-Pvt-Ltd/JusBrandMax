/**
 * jusBrandMax Cowork — MCP server + Agent Skill entry point.
 *
 * The stdio MCP server (tools: run_brand_report, get_history, list_competitors)
 * and the SKILL.md manifest are wired up in M5. For now this re-exports the
 * engine version so the package has a real, buildable surface.
 */

export { VERSION } from "@jusbrandmax/engine";
